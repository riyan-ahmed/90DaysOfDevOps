apiVersion: v1
kind: Service
metadata:
  name: day56-nginx-headless
  namespace: default
spec:
  clusterIP: None

  selector:
    app: day56-stateful-nginx

  ports:
    - name: http
      port: 80
      targetPort: 80

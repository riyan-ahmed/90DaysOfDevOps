apiVersion: v1
kind: Pod
metadata:
  name: day56-dns-test
  namespace: default
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command:
        - sleep
        - "3600"
  restartPolicy: Never
